<?xml version="1.0" encoding="utf-8"?>
<!--
  Open-source FM for Titan 2. Package id matches stock/MediaTek FM so we can
  replace AguiFMRadio on the same priv-app path without a second package.
  Sources are ours (Apache-2.0); only the JNI blob is vendor.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.fmradio"
    android:versionCode="112"
    android:versionName="1.0.12-bt">
    <uses-sdk android:minSdkVersion="28" android:targetSdkVersion="34"/>

    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_ROUTING"/>
    <uses-permission android:name="android.permission.CAPTURE_AUDIO_OUTPUT"/>
    <uses-permission android:name="android.permission.ACCESS_BROADCAST_RADIO"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:theme="@style/Theme.Titan.DayNight"
        android:hardwareAccelerated="true"
        android:extractNativeLibs="true"
        android:supportsRtl="true">

        <!-- Primary launcher: our UI -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden"
            android:theme="@style/Theme.Titan.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Survives screen-off; holds engine + wakelock + media FGS notification -->
        <service
            android:name=".FmService"
            android:exported="false"
            android:enabled="true"
            android:stopWithTask="false"
            android:foregroundServiceType="mediaPlayback">
            <!-- Explicit action so PMS always registers a service intent filter -->
            <intent-filter>
                <action android:name="com.android.fmradio.action.POWER_ON"/>
                <action android:name="com.android.fmradio.action.POWER_OFF"/>
                <action android:name="com.android.fmradio.action.TOGGLE"/>
            </intent-filter>
        </service>
    </application>
</manifest>
