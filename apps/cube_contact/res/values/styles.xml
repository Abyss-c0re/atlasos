<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Opaque full black — no wallpaper show-through on IME / GL resize. -->
    <style name="Theme.CubeContact.Opaque" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
        <item name="android:windowShowWallpaper">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <!-- Subdisplay residual: no dim wash / no translucent format. -->
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowDisablePreview">true</item>
        <!-- Titan cyberdeck: hardware keyboard only — never summon soft IME. -->
        <item name="android:windowSoftInputMode">adjustNothing|stateAlwaysHidden</item>
    </style>
</resources>
