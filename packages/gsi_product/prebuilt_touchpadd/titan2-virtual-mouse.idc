# titan2-touchpadd / titan2-orient-mouse (relative mouse).
# Do NOT set touch.deviceType=pointer (that is absolute tablet).
# REL_WHEEL (not HI_RES-only) is what InputReader maps to AXIS_VSCROLL.
device.internal = 1
